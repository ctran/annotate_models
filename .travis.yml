sudo: false
language: ruby
rvm:
  - 2.1.10
  - 2.2.6
  - 2.3.3
  - 2.4.0
  - ruby-head
matrix:
  allow_failures:
    - rvm: ruby-head
before_install:
  - gem update --system
  - gem update bundler
script:
  - bundle exec rspec && bundle exec rubocop
